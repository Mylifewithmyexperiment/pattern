<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="@dimen/app_margin_min">

            <ImageView
                android:id="@+id/ivBenefit"
                android:layout_width="@dimen/_30sdp"
                android:layout_height="@dimen/_30sdp"
                android:src="@drawable/ic_connection_lost" />

            <TextView
                android:id="@+id/tvTitle"
                style="@style/AppTheme.TextView"
                android:gravity="center"
                android:maxLines="2"
                android:layout_marginTop="@dimen/app_margin_min"
                android:singleLine="false"
                android:text="Movie, Music $ \nShopping"
                android:textSize="@dimen/app_text_min_medium" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:padding="@dimen/app_margin_min">


            <TextView
                android:id="@+id/tvDescription"
                style="@style/AppTheme.TextView"
                android:maxLines="4"
                android:textColor="@color/app_default_text_color"
                android:singleLine="false"
                android:text="Complimentry Mobile insurance for life-damage repair + antivirus + extended warranty + Free pick-up and drop"
                android:textSize="@dimen/app_text_medium" />

        </LinearLayout>


    </LinearLayout>

</layout>